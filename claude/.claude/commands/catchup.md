컨텍스트 윈도우를 리로드한다. /clear 후 현재 작업 상태를 빠르게 복구할 때 사용.

## 절차

1. `git status`로 현재 상태 확인
2. `git diff HEAD`로 커밋되지 않은 모든 변경 사항 읽기
3. `git log --oneline -10`으로 최근 작업 흐름 파악
4. CLAUDE.md가 있으면 읽기
5. 현재 진행 중인 작업을 간결하게 요약:
   - 어떤 파일이 변경되었는지
   - 변경의 목적이 무엇으로 보이는지
   - 다음에 해야 할 작업이 무엇인지
